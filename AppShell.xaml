<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       x:Class="SaveUp.AppShell">

    <ShellItem>
        <ShellContent Title="Home" ContentTemplate="{DataTemplate local:MainPage}" />
        <ShellContent Title="Add Product" ContentTemplate="{DataTemplate local:AddProductPage}" />
        <ShellContent Title="Product List" ContentTemplate="{DataTemplate local:ProductListPage}" />
    </ShellItem>
</Shell>
